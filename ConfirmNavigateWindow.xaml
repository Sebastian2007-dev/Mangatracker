<?xml version="1.0" encoding="utf-8"?>


<Window x:Class="MangaTracker.ConfirmNavigateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Domain-Wechsel bestätigen"
        Height="180"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText"
                   TextWrapping="Wrap"
                   FontSize="14"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="Erlauben" Width="100" Margin="0,0,8,0" Click="Allow_Click"/>
            <Button Content="Blockieren" Width="100" Margin="0,0,8,0" Click="Block_Click"/>
            <Button Content="Ja" Width="80" Margin="0,0,8,0" Click="Yes_Click"/>
            <Button Content="Nein" Width="80" Click="No_Click"/>
        </StackPanel>
    </Grid>
</Window>
