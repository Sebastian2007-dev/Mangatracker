<Window x:Class="MangaTracker.EditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manga bearbeiten"
        Height="265"
        Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Titel:" Margin="0,0,0,2" Grid.Row="0"/>
        <TextBox x:Name="TitleBox" Margin="0,0,0,10" Grid.Row="1"/>

        <TextBlock Text="URL:" Margin="0,0,0,2" Grid.Row="2"/>
        <TextBox x:Name="UrlBox" Margin="0,0,0,10" Grid.Row="3"/>

        <TextBlock Text="Kapitel-URL:" Margin="0,0,0,2" Grid.Row="4"/>
        <TextBox x:Name="ChapterUrlBox" Margin="0,0,0,10" Grid.Row="5"/>

        <Button Content="Speichern"
                Grid.Row="6"
                HorizontalAlignment="Right"
                Click="SaveButton_Click"/>
    </Grid>
</Window>
